import Logo from '@ui/logo/logo';
import { Container } from '@ui/container/container';

const Header = () => {
  return (
    <header>
      <Container>
        <Logo />
      </Container>
    </header>
  );
};

export default Header;
